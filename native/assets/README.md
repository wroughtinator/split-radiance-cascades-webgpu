Place `sponza.rcb` here for a self-contained development run, or set the
`SPLIT_RC_ASSETS` environment variable to the directory containing it.

The repository's canonical asset is `../../public/models/sponza.rcb`.
